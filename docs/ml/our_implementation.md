# Наша реализация алгоритма Holt-Winters

## 🏗️ Архитектура реализации

### Структура проекта C++


**cpp/ml/**
- **include/** - заголовочные файлы
  - `holt_winters.h` - основной класс алгоритма
  - `metrics.h` - метрики качества (WAPE, MAE, RMSE)
  - `time_series.h` - работа с временными рядами
- **src/** - исходные файлы
  - Основные модули:
    - `holt_winters.cpp` - реализация алгоритма
    - `metrics.cpp` - реализация метрик
    - `time_series.cpp` - загрузка и обработка данных
    - `main_ml.cpp` - основная программа тестирования
    - `performance_benchmark.cpp` - бенчмарк производительности
  - Настройка параметров:
    - `first_tuning.cpp` - этап 1: грубый подбор
    - `second_tuning.cpp` - этап 2: менее грубый подбор
    - `third_tuning.cpp` - этап 3: детальный подбор
    - `forth_tuning.cpp` - этап 4: самый точный подбор
- `CMakeLists.txt` - файл сборки CMake


## 🔧 Ключевые особенности реализации

### Класс HoltWinters

```cpp
class HoltWinters {
public:
    HoltWinters(int season_length = 7);
    bool fit(const std::vector<double>& data, double alpha, double beta, double gamma);
    std::vector<double> predict(int horizon) const;
    
private:
    int season_length;
    double level, trend;
    std::vector<double> seasonal;
    void initializeComponents(const std::vector<double>& data);
};

```

## 🔧 Ключевые особенности реализации

### Особенности реализации

1. **Стабильная инициализация** - робастные методы инициализации компонент
2. **Защита от расходимости** - проверки на корректность значений
3. **Обработка ошибок** - валидация входных параметров
4. **Модульность** - разделение на логические компоненты

## 📊 Метрики качества

### Реализованные метрики

```cpp
class Metrics {
public:
    static double wape(const std::vector<double>& actual, 
                      const std::vector<double>& predicted);
    static double mae(const std::vector<double>& actual,
                     const std::vector<double>& predicted);
    static double rmse(const std::vector<double>& actual,
                      const std::vector<double>& predicted);
};

```



## WAPE формула **WAPE = (Σ|actual - predicted| / Σ|actual|) × 100%**

## 🎯 Оптимальные параметры

В результате тонкой настройки найдены оптимальные параметры:

| Параметр | Значение | Описание |
|----------|----------|----------|
| **α** | 0.07 | Сглаживание уровня |
| **β** | 0.01 | Сглаживание тренда |
| **γ** | 0.07 | Сглаживание сезонности |
| **m** | 7 | Длина сезонного цикла (неделя) |
| **train_ratio** | 0.7 | Доля обучающей выборки |


## 📈 Производительность

### Временная сложность
- **Обучение:** ~0.15 мс на точку данных
- **Прогноз:** ~0.02 мс на точку прогноза

### Использование памяти
- **Модель:** ~0.5 КБ базовой памяти + 0.01 КБ на точку данных

## 🧪 Тестирование

### Модульные тесты
- Корректность инициализации компонент
- Проверка расчетов прогнозов
- Валидация метрик качества


### Интеграционные тесты
- End-to-end тестирование на реальных данных

## 🔄 Процесс разработки

1. **Базовая реализация** - реализация core алгоритма
2. **Оптимизация** - улучшение стабильности и точности
3. **Настройка параметров** - поиск оптимальных значений
4. **Документирование** - создание полной документации

## ✅ Достигнутые результаты

- **WAPE:** 11.41% (цель: < 12%) ✅
- **Стабильность:** устойчивая работа на различных данных
- **Производительность:** линейная сложность O(n)
- **Качество кода:** модульная архитектура, обработка ошибок