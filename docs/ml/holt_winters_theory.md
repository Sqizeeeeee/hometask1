# Теоретическая справка: Алгоритм Holt-Winters

## 📊 Общее описание

**Тройное экспоненциальное сглаживание (Holt-Winters)** - это метод прогнозирования временных рядов, который учитывает три компоненты:
- **Уровень (Level)** - базовое значение ряда
- **Тренд (Trend)** - направление изменения ряда  
- **Сезонность (Seasonality)** - периодические колебания

## 🧮 Математическая основа

### Аддитивная модель Holt-Winters

#### Основные уравнения:

**Обновление компонент:**

Уровень: l_t = α × (y_t - s_{t-m}) + (1-α) × (l_{t-1} + b_{t-1})  
Тренд: b_t = β × (l_t - l_{t-1}) + (1-β) × b_{t-1}  
Сезонность: s_t = γ × (y_t - l_t) + (1-γ) × s_{t-m}  


**Прогноз на h шагов вперед:**

y_{t+h} = l_t + h × b_t + s_{t+h-m(k+1)}  

где m - длина сезонного цикла, k - целая часть (h-1)/m

### Параметры алгоритма

- **α (alpha)** - параметр сглаживания для уровня (0 < α < 1)
- **β (beta)** - параметр сглаживания для тренда (0 < β < 1)  
- **γ (gamma)** - параметр сглаживания для сезонности (0 < γ < 1)
- **m** - длина сезонного цикла (например, 7 для недельной сезонности)

## ⚡ Вычислительная сложность

### Временная сложность
- **Обучение:** O(n × m) - где n длина ряда, m количество итераций оптимизации
- **Прогноз:** O(h) - где h горизонт прогнозирования

### Пространственная сложность  
- **Хранение модели:** O(m) - для сезонных компонент
- **Данные:** O(n) - для временного ряда

## 📈 Области применения

- Прогнозирование продаж
- Предсказание трафика веб-сайтов
- Анализ энергопотребления
- Экономическое прогнозирование

## ✅ Преимущества

- Учитывает тренд и сезонность
- Простота реализации
- Быстрое выполнение
- Интерпретируемость результатов

## ⚠️ Ограничения

- Предполагает аддитивную сезонность
- Чувствительность к выбору параметров
- Требует достаточного количества данных (минимум 2 сезонных цикла)

## 🔍 Метрики качества

- **WAPE (Weighted Absolute Percentage Error)** - взвешенная абсолютная процентная ошибка
- **MAE (Mean Absolute Error)** - средняя абсолютная ошибка  
- **RMSE (Root Mean Square Error)** - среднеквадратичная ошибка